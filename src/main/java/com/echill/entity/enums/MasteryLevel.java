package com.echill.entity.enums;

public enum MasteryLevel {
    BEGINNER,      // Mất gốc (Tỷ lệ <= 25%)
    INTERMEDIATE,  // Trung bình (Tỷ lệ 26% - 50%)
    ADVANCED,      // Khá giỏi (Tỷ lệ 51% - 75%)
    MASTER         // Phá đảo (Tỷ lệ > 75%)
}
